修正后的"今天就干这些"清单
基于所有材料和 GPT review，今天的核心目标就一个：跑通串行 Pipeline + 验证稀疏对精度的影响。

🔴 今天必须完成（3 件事）
1. 项目骨架 + GOP 解析（~1-2 小时）
创建项目目录结构
用 PyAV 写 gop_parser.py：读视频 → 输出 GOP 列表（每个 GOP 的 I 帧 pts、size）
跑一个测试视频，打印 GOP 列表，确认能正确解析
2. 串行版 Baseline 跑通（~2-3 小时）
frame_decoder.py：解码 I 帧 → Resize 到 336×336
inference.py：加载 Qwen2-VL → 接收帧 → 推理 → 输出文本
main.py：串行串起来：读视频 → 解码全部 I 帧 → Resize → 推理 → 输出
记录 baseline 数据：端到端延迟、每阶段耗时、准确率（随便跑几条 QA）
注意：显存清理先写三步骤（del + gc + empty_cache），但标注为"待验证"
3. 稀疏化 + 精度初测（~2-3 小时）
这是导师最关心的，也是 GPT 说的"应该提前验证"的核心：

audio_energy.py：librosa 提取音频 RMS
sparse.py：AV-LRM 打分 + Top-K / 均匀采样
关键实验：在同一组测试视频上，对比：
全部 I 帧（baseline）
稀疏化后的 I 帧（alpha=0.5，K=总 GOP 数的 50%）
看精度掉了多少、延迟降了多少
P 帧：今天只预留函数签名 sample_p_frames(gop, strategy) → List[Frame]，不实现